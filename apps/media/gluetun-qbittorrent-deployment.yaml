apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qbittorrent
  template:
    metadata:
      labels:
        app: qbittorrent
    spec:
      containers:
      - name: gluetun
        image: qmcgaw/gluetun:latest
        securityContext:
          capabilities:
            add: ["NET_ADMIN"]
        env:
        - name: VPN_SERVICE_PROVIDER
          value: "mullvad"
        - name: VPN_TYPE
          value: "wireguard"
        - name: WIREGUARD_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: mullvad-credentials
              key: WIREGUARD_PRIVATE_KEY
        - name: WIREGUARD_ADDRESSES
          valueFrom:
            secretKeyRef:
              name: mullvad-credentials
              key: WIREGUARD_ADDRESSES
        - name: DOT
          value: "off"
        - name: DNS_ADDRESS
          value: "1.1.1.1"
        - name: BLOCK_MALICIOUS
          value: "off"
        - name: BLOCK_ADS
          value: "off"
        - name: BLOCK_SURVEILLANCE
          value: "off"
        ports:
        - containerPort: 8080
      - name: qbittorrent
        image: lscr.io/linuxserver/qbittorrent:latest
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "Europe/Stockholm"
        - name: WEBUI_PORT
          value: "8080"
        lifecycle:
          postStart:
            exec:
              command:
              - sh
              - -c
              - |
                sleep 5
                grep -q "HostHeaderValidation" /config/qBittorrent/qBittorrent.conf || echo 'WebUI\HostHeaderValidation=false' >> /config/qBittorrent/qBittorrent.conf
                grep -q "AuthSubnetWhitelistEnabled" /config/qBittorrent/qBittorrent.conf || echo 'WebUI\AuthSubnetWhitelistEnabled=true' >> /config/qBittorrent/qBittorrent.conf
                grep -q "AuthSubnetWhitelist=" /config/qBittorrent/qBittorrent.conf || echo 'WebUI\AuthSubnetWhitelist=192.168.1.0/24, 10.244.0.0/16' >> /config/qBittorrent/qBittorrent.conf
        volumeMounts:
        - name: config
          mountPath: /config
        - name: downloads
          mountPath: /media
      volumes:
      - name: config
        persistentVolumeClaim:
          claimName: qbittorrent-config-pvc
      - name: downloads
        persistentVolumeClaim:
          claimName: media-library-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: media
spec:
  type: NodePort
  selector:
    app: qbittorrent
  ports:
  - port: 8080
    targetPort: 8080