- name: Baseline configuration for all nodes
  hosts: all
  become: true
  roles:
    - common

- name: Kubernetes node prerequisites
  hosts: control_plane:workers
  become: true
  roles:
    - k8s-node-prereqs

- name: GPU driver setup
  hosts: gpu_nodes
  become: true
  roles:
    - nvidia-gpu